<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.yakirhelp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Yakirhelp">
        <activity android:name=".browse_recipes"></activity>
        <activity android:name=".personal_page" />
        <activity android:name=".register_activity" />
        <activity android:name=".main_screen_activity" />
        <activity android:name=".VitaminTable" />
        <activity android:name=".RecipePage" />
        <activity android:name=".login_activity" />
        <activity android:name=".AddRecipe" />
        <activity android:name=".temp_screen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>